import streamlit as st
import pandas as pd
import os

st.set_page_config(page_title="Анализ ниш Wildberries", layout="wide")

def format_number(x):
    if pd.isna(x) or x == 0:
        return "—"
    return f"{int(x):,}".replace(",", " ")

@st.cache_data(ttl=3600)
def load_data():
    # Рынок
    market = pd.read_excel("пример.xlsx", sheet_name="Предметы")
    queries = pd.read_excel("пример.xlsx", sheet_name="Запросы")
    
    # Продажи
    sales_list = []
    for fname in ["ЦР_Продажи.xlsx", "МС_Продажи.xlsx"]:
        if os.path.exists(fname):
            df = pd.read_excel(fname, sheet_name="Товары")
            df["Юрлицо"] = fname.split("_")[0]
            sales_list.append(df)
        else:
            st.warning(f"⚠️ Файл {fname} не найден")

    if not sales_list:
        st.error("❌ Нет данных по продажам")
        st.stop()

    sales = pd.concat(sales_list, ignore_index=True)
    return market, queries, sales

market, queries, sales = load_data()

# Агрегация ваших продаж
sales_agg = sales.groupby(['Предмет', 'Юрлицо'], as_index=False).agg(
    Мои_заказы=('Заказали на сумму, ₽', 'sum'),
    Мои_товары=('Артикул WB', 'count'),
    Мой_процент_выкупа=('Процент выкупа', 'mean')
)

# Агрегация запросов
queries_agg = queries.groupby('Предмет', as_index=False).agg(
    Количество_запросов=('Количество запросов', 'sum')
)

# Объединение с рыночными данными
base = market[[
    'Предмет', 'Продавцы', 'Продавцы с заказами', 'Монополизация, %',
    'Выручка, ₽', '%  прироста выручки', 'Средний чек, ₽',
    'Оборачиваемость за неделю, дни', 'Процент выкупа'
]].copy()

base = pd.merge(base, queries_agg, on='Предмет', how='left')
base = pd.merge(base, sales_agg, on='Предмет', how='left')

# Заполняем пропуски
for col in ['Мои_заказы', 'Мои_товары', 'Мой_процент_выкупа', 'Количество_запросов']:
    base[col] = base[col].fillna(0)

# Доля рынка
base['Моя_доля_рынка_%'] = (base['Мои_заказы'] / base['Выручка, ₽'].replace(0, 1) * 100).round(2)

# -------------------------------
# НАСТРОЙКИ РЕКОМЕНДАЦИЙ
# -------------------------------
st.sidebar.title("⚙️ Настройки рекомендаций")
min_growth = st.sidebar.number_input("Мин. рост выручки (%)", value=20, step=5)
max_monopoly = st.sidebar.number_input("Макс. монополизация (%)", value=50, step=5)
min_queries = st.sidebar.number_input("Мин. запросов", value=100000, step=10000)
max_turnover = st.sidebar.number_input("Макс. оборачиваемость (дни)", value=30, step=5)
min_buyout = st.sidebar.number_input("Мин. выкуп (%)", value=70, step=5)

# -------------------------------
# ФИЛЬТР ПО ЮРЛИЦУ
# -------------------------------
legal_entities = ['Любое'] + sorted(base['Юрлицо'].dropna().unique())
selected_legal = st.sidebar.selectbox("Юрлицо", legal_entities)

if selected_legal == "Любое":
    agg_all = base.groupby('Предмет', as_index=False).agg(
        Выручка_RUB=('Выручка, ₽', 'first'),
        Количество_запросов=('Количество_запросов', 'first'),
        Монополизация=('Монополизация, %', 'first'),
        Продавцы=('Продавцы', 'first'),
        Продавцы_с_заказами=('Продавцы с заказами', 'first'),
        Оборачиваемость=('Оборачиваемость за неделю, дни', 'first'),
        Процент_выкупа=('Процент выкупа', 'first'),
        Мои_заказы=('Мои_заказы', 'sum'),
        Юрлица=('Юрлицо', lambda x: ', '.join(sorted(x.dropna().unique()))),
        Моя_доля_рынка=('Моя_доля_рынка_%', 'mean'),
        Мой_процент_выкупа=('Мой_процент_выкупа', 'mean')
    )
    df = agg_all
else:
    df = base[base['Юрлицо'] == selected_legal].copy()
    df['Юрлица'] = selected_legal

# -------------------------------
# РЕКОМЕНДАЦИИ
# -------------------------------
def get_rec(row):
    if row['Мои_заказы'] == 0:
        if (row['Количество_запросов'] >= min_queries and
            row['Монополизация'] <= max_monopoly and
            row['%  прироста выручки'] >= min_growth and
            row['Оборачиваемость'] <= max_turnover):
            return "✅ Вход"
        else:
            return "⏸ Не сейчас"
    else:
        if (row['Моя_доля_рынка'] < 5 and
            row['%  прироста выручки'] >= min_growth and
            row['Мой_процент_выкупа'] >= min_buyout):
            return "🚀 Усиление"
        elif row['Мой_процент_выкупа'] < 70:
            return "⚠️ Выход / Анализ"
        else:
            return "📊 Мониторинг"

df['%  прироста выручки'] = df['%  прироста выручки'].fillna(0)
df['Рекомендация'] = df.apply(get_rec, axis=1)

# -------------------------------
# ФИЛЬТР ПО РЕКОМЕНДАЦИИ
# -------------------------------
rec_options = sorted(df['Рекомендация'].unique())
selected_recs = st.sidebar.multiselect("Рекомендация", rec_options, default=rec_options)
df = df[df['Рекомендация'].isin(selected_recs)].copy()

# -------------------------------
# СОРТИРОВКА ПО ЧИСЛОВЫМ КОЛОНКАМ
# -------------------------------
df = df.sort_values('Выручка_RUB' if 'Выручка_RUB' in df.columns else 'Выручка, ₽', ascending=False).reset_index(drop=True)

# -------------------------------
# ОТОБРАЖЕНИЕ
# -------------------------------
st.title("🔍 Анализ ниш Wildberries")

display_df = df.copy()
display_df['Выручка, ₽'] = display_df.get('Выручка, ₽', display_df.get('Выручка_RUB', 0)).apply(format_number)
display_df['Количество_запросов'] = display_df['Количество_запросов'].apply(format_number)
display_df['Мои_заказы'] = display_df['Мои_заказы'].apply(format_number)

st.dataframe(
    display_df[[
        'Предмет', 'Юрлица', 'Выручка, ₽', 'Количество_запросов', 'Монополизация',
        'Продавцы_с_заказами', 'Мои_заказы', 'Моя_доля_рынка', 'Мой_процент_выкупа', 'Рекомендация'
    ]].rename(columns={
        'Монополизация': 'Монополизация, %',
        'Продавцы_с_заказами': 'Продавцы с заказами',
        'Моя_доля_рынка': 'Моя_доля_рынка_%'
    }),
    use_container_width=True,
    hide_index=True
)

# -------------------------------
# ЗАПРОСЫ ПО ПРЕДМЕТУ
# -------------------------------
st.subheader("🔎 Запросы по предмету")
subjects = sorted(df['Предмет'].dropna().unique())
selected_subject = st.selectbox("Выберите предмет", subjects)

if selected_subject:
    q_filtered = queries[queries['Предмет'] == selected_subject].copy()
    q_filtered = q_filtered.sort_values('Заказали товаров', ascending=False)
    st.dataframe(
        q_filtered[['Поисковый запрос', 'Количество запросов', 'Заказали товаров']],
        use_container_width=True
    )
import streamlit as st
import pandas as pd
import os

# -------------------------------
# Настройка страницы
# -------------------------------
st.set_page_config(
    page_title="Анализ ниш Wildberries",
    layout="wide"
)

# -------------------------------
# ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
# -------------------------------
def format_number(x):
    if pd.isna(x) or x == 0:
        return "—"
    return f"{int(x):,}".replace(",", " ")

def safe_join(x):
    valid = x.dropna().astype(str).unique()
    return ', '.join(sorted(valid)) if len(valid) > 0 else "—"

# -------------------------------
# ЗАГРУЗКА И ПОДГОТОВКА ДАННЫХ
# -------------------------------
@st.cache_data(ttl=3600)
def load_market_data():
    market = pd.read_excel("пример.xlsx", sheet_name="Предметы")
    queries = pd.read_excel("пример.xlsx", sheet_name="Запросы")
    return market, queries

@st.cache_data(ttl=3600)
def load_sales_data():
    files = ["ЦР_Продажи.xlsx", "МС_Продажи.xlsx"]
    all_sales = []
    for file in files:
        if os.path.exists(file):
            df = pd.read_excel(file, sheet_name="Товары")
            df["Юрлицо"] = file.split("_")[0]
            all_sales.append(df)
        else:
            st.warning(f"⚠️ Файл {file} не найден")
    if not all_sales:
        st.error("❌ Нет данных по продажам")
        st.stop()
    return pd.concat(all_sales, ignore_index=True)

# -------------------------------
# ОСНОВНАЯ ЛОГИКА
# -------------------------------
market, queries = load_market_data()
sales = load_sales_data()

# -------------------------------
# АГРЕГАЦИЯ ПРОДАЖ ПО ПРЕДМЕТУ И ЮРЛИЦУ
# -------------------------------
# Убедимся, что числовые колонки — числа
sales['Заказали на сумму, ₽'] = pd.to_numeric(sales['Заказали на сумму, ₽'], errors='coerce').fillna(0)
sales['Выкупили на сумму, ₽'] = pd.to_numeric(sales['Выкупили на сумму, ₽'], errors='coerce').fillna(0)

# Агрегация без дублирования
sales_agg = sales.groupby(['Предмет', 'Юрлицо']).agg(
    Мои_заказы=('Заказали на сумму, ₽', 'sum'),
    Мои_выкупы=('Выкупили на сумму, ₽', 'sum'),
    Мои_товары=('Артикул WB', 'count')
).round(2).reset_index()

# Корректный расчёт выкупа только там, где есть заказы
sales_agg['Мой_выкуп_процент'] = 0.0
mask = sales_agg['Мои_заказы'] > 0
sales_agg.loc[mask, 'Мой_выкуп_процент'] = (
    sales_agg.loc[mask, 'Мои_выкупы'] / sales_agg.loc[mask, 'Мои_заказы'] * 100
).round(2)

# -------------------------------
# АГРЕГАЦИЯ ЗАПРОСОВ ПО ПРЕДМЕТУ
# -------------------------------
queries_agg = queries.groupby('Предмет')['Количество запросов'].sum().reset_index()
queries_agg['Количество запросов'] = pd.to_numeric(queries_agg['Количество запросов'], errors='coerce').fillna(0)
queries_agg.rename(columns={'Количество запросов': 'Количество_запросов'}, inplace=True)

# -------------------------------
# ОБЪЕДИНЕНИЕ РЫНОЧНЫХ ДАННЫХ
# -------------------------------
base = market[[
    'Предмет', 'Продавцы', 'Продавцы с заказами', 'Монополизация, %',
    'Выручка, ₽', '%  прироста выручки', 'Средний чек, ₽',
    'Оборачиваемость за неделю, дни', 'Процент выкупа'
]]

# -------------------------------
# НАСТРОЙКИ РЕКОМЕНДАЦИЙ В SIDEBAR
# -------------------------------
st.sidebar.title("⚙️ Настройки рекомендаций")
min_growth = st.sidebar.number_input("Мин. рост выручки (%)", value=20, step=5)
max_monopoly = st.sidebar.number_input("Макс. монополизация (%)", value=50, step=5)
min_queries = st.sidebar.number_input("Мин. запросов", value=100000, step=10000)
max_turnover = st.sidebar.number_input("Макс. оборачиваемость (дни)", value=30, step=5)
min_buyout = st.sidebar.number_input("Мин. выкуп (%)", value=70, step=5)

# -------------------------------
# ФИЛЬТР ПО ЮРЛИЦУ
# -------------------------------
legal_entities = sorted(sales_agg["Юрлицо"].dropna().unique())
selected_legal = st.sidebar.selectbox("Юрлицо", ["Любое"] + legal_entities)

# -------------------------------
# ОБЪЕДИНЕНИЕ БЕЗ ДУБЛИРОВАНИЯ
# -------------------------------
if selected_legal == "Любое":
    combined = sales_agg.groupby('Предмет').agg(
        Мои_заказы=('Мои_заказы', 'sum'),
        Мои_товары=('Мои_товары', 'sum'),
        Мой_выкуп_процент=('Мой_выкуп_процент', 'mean'),
        Юрлица=('Юрлицо', safe_join)
    ).round(2).reset_index()
    result = base.merge(combined, on='Предмет', how='left')
    result['Юрлица'] = result['Юрлица'].fillna("—")
else:
    filtered = sales_agg[sales_agg['Юрлицо'] == selected_legal]
    result = base.merge(filtered, on='Предмет', how='left')
    result = result.copy()
    result['Юрлица'] = selected_legal

# Заполняем пропуски числовыми нулями
for col in ['Мои_заказы', 'Мои_товары', 'Мой_выкуп_процент']:
    result[col] = result[col].fillna(0)

# Расчёт доли рынка
result['Моя_доля_рынка_%'] = (result['Мои_заказы'] / result['Выручка, ₽'].replace(0, 1) * 100).round(2)

# -------------------------------
# РЕКОМЕНДАЦИИ
# -------------------------------
def get_recommendation(row):
    if row['Мои_заказы'] == 0:
        if (row['Количество_запросов'] >= min_queries and
            row['Монополизация, %'] <= max_monopoly and
            row['%  прироста выручки'] >= min_growth and
            row['Оборачиваемость за неделю, дни'] <= max_turnover):
            return "✅ Вход"
        else:
            return "⏸ Не сейчас"
    else:
        if (row['Моя_доля_рынка_%'] < 5 and
            row['%  прироста выручки'] >= min_growth and
            row['Мой_выкуп_процент'] >= min_buyout):
            return "🚀 Усиление"
        elif row['Мой_выкуп_процент'] < 70:
            return "⚠️ Выход / Анализ"
        else:
            return "📊 Мониторинг"

result['Рекомендация'] = result.apply(get_recommendation, axis=1)

# -------------------------------
# ФИЛЬТР ПО РЕКОМЕНДАЦИИ
# -------------------------------
rec_options = sorted(result['Рекомендация'].unique())
selected_recs = st.sidebar.multiselect("Рекомендация", rec_options, default=rec_options)
result = result[result['Рекомендация'].isin(selected_recs)].copy()

# -------------------------------
# СОРТИРОВКА ПО ВЫРУЧКЕ (ЧИСЛОВАЯ!)
# -------------------------------
result = result.sort_values('Выручка, ₽', ascending=False).reset_index(drop=True)

# -------------------------------
# ОТОБРАЖЕНИЕ ТАБЛИЦЫ
# -------------------------------
st.title("🔍 Анализ ниш Wildberries")

# Форматирование только при отображении
df_display = result.copy()
for col in ['Выручка, ₽', 'Количество_запросов', 'Мои_заказы']:
    df_display[col] = df_display[col].apply(format_number)

st.dataframe(
    df_display[[
        'Предмет', 'Юрлица', 'Выручка, ₽', 'Количество_запросов', 'Монополизация, %',
        'Продавцы с заказами', 'Мои_заказы', 'Моя_доля_рынка_%', 'Мой_выкуп_процент', 'Рекомендация'
    ]].rename(columns={'Мои_заказы': 'Мои заказы, ₽'}),
    use_container_width=True,
    height=700,
    hide_index=True
)

# -------------------------------
# ЗАПРОСЫ ПО ПРЕДМЕТУ
# -------------------------------
st.subheader("🔎 Запросы по предмету")
subjects = sorted(result['Предмет'].dropna().unique())
selected_subject = st.selectbox("Выберите предмет", subjects)

if selected_subject:
    q_filtered = queries[queries['Предмет'] == selected_subject].copy()
    q_filtered['Заказали товаров'] = pd.to_numeric(q_filtered['Заказали товаров'], errors='coerce').fillna(0)
    q_filtered = q_filtered.sort_values('Заказали товаров', ascending=False)
    st.dataframe(
        q_filtered[['Поисковый запрос', 'Количество запросов', 'Заказали товаров', 'Конверсия в заказ']],
        use_container_width=True,
        height=600
    )